// Goalkeeper-only tactic for testing
// Only goalkeeper position is active, others are disabled

positions = [
  {
    type = goalkeeper;
    pose = {
      rotation = 0deg;
      translation = {x = xPosOwnGoalArea - 200; y = 0;};
    };
    stabilityOffset = 5000; // Maximum stability
  }
];

// Only goalkeeper in priority group
priorityGroups = [
  {
    positions = [goalkeeper];
    priorities = [1];
  }
];